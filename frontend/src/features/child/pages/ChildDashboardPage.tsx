import { Container, Typography } from "@mui/material";

export default function ChildDashboardPage() {
    return (
        <Container sx={{ py: 6 }}>
            <Typography variant="h4" fontWeight={700}>Child Dashboard</Typography>
        </Container>
    );
}