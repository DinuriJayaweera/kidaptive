import { Container, Typography } from "@mui/material";

export default function ParentDashboardPage() {
    return (
        <Container sx={{ py: 6 }}>
            <Typography variant="h4" fontWeight={700}>Parent Dashboard</Typography>
        </Container>
    );
}